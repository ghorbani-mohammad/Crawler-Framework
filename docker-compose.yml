version: '3.4'

services:
  crawler_db:
    container_name: crawler_db
    image: mdillon/postgis:11
    restart: unless-stopped
    volumes:
      - crawler_db:/var/lib/postgresql/data
  crawler:
    image: crawler
    container_name: crawler_django
    # command: ["gunicorn", "--bind", "0.0.0.0:8200", "app.wsgi:application"]
    command: ["python", "/app/manage.py", "runserver", "0.0.0.0:8200"]
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports: 
      - 8200:8200
    restart: unless-stopped
    # network_mode: host
    environment:
      DEBUG: 'True'
  
  redis:
    image: redis
    container_name: crawler_redis
    volumes: 
      - ./redis:/data
    restart: always
    # network_mode: host
    logging:
      driver: none
  
  celery_worker:
    image: crawler
    container_name: crawler_celery_worker
    volumes:
      - .:/app
    command: ["celery", "-A", "app", "worker", "-l", "info", "--pidfile="]
    restart: always
    # network_mode: host
    # logging:
    #   driver: none
  
  celery_scheduler:
    image: crawler
    container_name: crawler_celery_scheduler
    volumes:
      - .:/app
    command: ["celery", "-A", "app", "beat", "-l", "info", "--pidfile="]
    restart: always
    # network_mode: host
    # logging:
    #   driver: none
  
  chrome_browser:
    image: selenium/standalone-chrome
    container_name: crawler_chrome_browser
    logging:
      driver: none
    restart: always
    # network_mode: host
  
  
volumes:
  crawler_db: